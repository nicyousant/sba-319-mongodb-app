<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/script.js" defer></script>
</head>
<body>
    <h1>Edit a recommendation:</h1>
    <form action="/recommendations/<%= recommendation._id %>?_method=PUT" method="POST" class="form form-edit">
        <label for="place">
            Place:
            <input type="text" name="place" id="place" value="<%= recommendation.place %>">
        </label>
        <label for="reason">
            Reason:
            <input type="text" name="reason" id="reason" value="<%= recommendation.reason %>">
        </label>
   <label for="visit_when">
           When to Visit:
            <input type="text" name="visit_when" id="visit_when" value="<%= recommendation.visit_when %>">
        </label>
     
        <button>Update</button>
    </form>
    <br><br><br>
    <p>
        <a href="/recommendations/<%= recommendation._id %>">Back</a>
    </p>
</body>
</html>